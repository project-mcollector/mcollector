name: PR Checks

on:
  pull_request:
    branches:
      - main

jobs:
  check-pr:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@v3

      - name: Ensure PR is squashed
        uses: actions/github-script@v7
        with:
          github-token: ${{ secrets.GITHUB_TOKEN }}
          script: |
            const prNumber = context.payload.pull_request.number;
            const { data: commits } = await github.rest.pulls.listCommits({
              owner: context.repo.owner,
              repo: context.repo.repo,
              pull_number: prNumber
            });
            if (commits.length > 1) {
              core.setFailed('PR has multiple commits. Please squash before merging');
            }

      - name: Lint PR title
        uses: wagoid/commitlint-github-action@v4
        with:
          github_token: ${{ secrets.GITHUB_TOKEN }}
          configFile: .commitlintrc.js
          fail_on_non_conventional: true

      - name: Setup Node.js
        uses: actions/setup-node@v3
        with:
          node-version: 20

      - name: Setup pnpm
        uses: pnpm/action-setup@v4

      - name: Install dependencies
        run: pnpm install

      - name: Lint frontend
        run: pnpm --filter web run lint

      - name: Build frontend
        run: pnpm --filter web run build

      - name: Setup .NET
        uses: actions/setup-dotnet@v3
        with:
          dotnet-version: 10.0.x

      - name: Restore dependencies
        run: dotnet restore apps/api

      - name: Format Check
        run: dotnet format apps/api/Api.sln --verify-no-changes

      - name: Build API
        run: dotnet build apps/api --no-restore
